package models

/* import "gorm.io/gorm/clause"

type NoWait struct {
}

// Build build where clause
func (nowait NoWait) Build(builder clause.Builder) {
	builder.WriteString("NOWAIT")
}

// MergeClause merge order by clauses
func (nowait NoWait) MergeClause(clause *clause.Clause) {
	clause.BeforeExpression = clause.Locking
}
*/
