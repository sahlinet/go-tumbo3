apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-tumbo3.fullname" . }}-test-connection"
  labels:
    {{- include "go-tumbo3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-tumbo3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
